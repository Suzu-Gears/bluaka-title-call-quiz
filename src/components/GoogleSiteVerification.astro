---
import { PUBLIC_GS_VERIFICATION_ID } from '@/server-constants'

const verificationId = PUBLIC_GS_VERIFICATION_ID
---

{
  verificationId && (
    <meta name="google-site-verification" content={verificationId} />
  )
}
