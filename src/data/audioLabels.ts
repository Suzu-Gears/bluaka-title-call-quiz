/**
 * タイトルコールクリップの表示名。カード一覧・答え合わせ後・リザルトでのみ
 * 表示する(出題中は答えのヒントになるため表示しない)。
 *
 * キーは `${clipId}.g${世代}`。フォルダ(生徒Id)を含めないため、
 * クリップを別メンバーのフォルダへ移動しても表示名は追従する。
 *
 * 例:
 *   'cherino_title.g1': '旧声優版',
 *   'cherino_title.g2': '現行版',
 *
 * クリップの「どの形態の声か」はここでは扱わない。R2 上の置き場所
 * (audio/{生徒Id}/...)がそのまま帰属の宣言になる。
 */
export const AUDIO_CLIP_LABELS: Record<string, string> = {}
